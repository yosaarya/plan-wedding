/** Konstanta domain. Tidak ada nilai ajaib yang ditulis langsung di komponen (aturan B1.4). */

/** Batas anggota dengan hak tulis per pernikahan (aturan A1.3). */
export const MAX_EDITORS_PER_WEDDING = 2

/** Panjang token RSVP yang dihasilkan database (aturan A5.10). */
export const RSVP_TOKEN_LENGTH = 32

/** Baris per halaman untuk daftar panjang (aturan B6.1). */
export const PAGE_SIZE = 50

/** Jumlah tugas yang ditampilkan di kartu "Tugas Bulan Ini" (aturan A3.5). */
export const HOME_TASK_LIMIT = 5

/** Ambang peringatan pemakaian anggaran, dalam persen (aturan A4.7). */
export const BUDGET_WARNING_PERCENT = 90

/** Panjang maksimum ucapan dari halaman RSVP (aturan A5.15). */
export const MAX_WISH_LENGTH = 500

/** Zona waktu default pernikahan. */
export const DEFAULT_TIMEZONE = 'Asia/Jakarta'
